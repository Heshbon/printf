#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>

int _putchar(char c);
int printf_c(char s);
int putstring(char *b);
int _printf(const char *format, ...);

#endif
